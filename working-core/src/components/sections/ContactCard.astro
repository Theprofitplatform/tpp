---
// Contact information card with equal visual weight to form
---

<aside class="contact-card" role="complementary" aria-labelledby="contact-title">
  <h3 class="contact-title" id="contact-title">Get in Touch</h3>
  <p class="contact-subtitle">Prefer to speak directly? We're here to help.</p>

  <div class="contact-methods">
    <!-- Phone Contact -->
    <div class="contact-item">
      <div class="contact-icon" aria-hidden="true">
        <i class="fas fa-phone"></i>
      </div>
      <div class="contact-content">
        <div class="contact-label">Call Direct</div>
        <a href="tel:+61487286451" class="contact-link" aria-describedby="phone-desc">
          0487 286 451
        </a>
        <div id="phone-desc" class="contact-desc">Instant response during business hours</div>
      </div>
    </div>

    <!-- Email Contact -->
    <div class="contact-item">
      <div class="contact-icon" aria-hidden="true">
        <i class="fas fa-envelope"></i>
      </div>
      <div class="contact-content">
        <div class="contact-label">Email</div>
        <a href="mailto:avi@theprofitplatform.com.au" class="contact-link" aria-describedby="email-desc">
          avi@theprofitplatform.com.au
        </a>
        <div id="email-desc" class="contact-desc">Detailed questions welcome</div>
      </div>
    </div>

    <!-- Business Hours -->
    <div class="contact-item">
      <div class="contact-icon" aria-hidden="true">
        <i class="fas fa-clock"></i>
      </div>
      <div class="contact-content">
        <div class="contact-label">Business Hours</div>
        <div class="contact-value">
          <div>Mon-Fri: 9AM-6PM AEST</div>
          <div class="hours-note">Weekend: Emergency only</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Trust Indicators -->
  <div class="contact-trust">
    <div class="trust-badge">
      <i class="fas fa-award" aria-hidden="true"></i>
      <span>Local Sydney Expert</span>
    </div>
    <div class="trust-badge">
      <i class="fas fa-users" aria-hidden="true"></i>
      <span>15+ Happy Clients</span>
    </div>
  </div>

  <!-- Quick CTA -->
  <div class="contact-cta">
    <a
      href="tel:+61487286451"
      class="btn-contact"
      aria-describedby="cta-desc"
    >
      <i class="fas fa-phone" aria-hidden="true"></i>
      <span>Call Now</span>
    </a>
    <div id="cta-desc" class="sr-only">Call now for immediate assistance</div>
  </div>
</aside>

<style>
.contact-card {
  background: white;
  border-radius: 16px;
  padding: 2rem;
  box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
  border: 1px solid #e2e8f0;
  height: fit-content;
  position: sticky;
  top: 2rem;
}

.contact-title {
  font-size: 1.5rem;
  font-weight: 600;
  color: #1e293b;
  margin: 0 0 0.5rem 0;
}

.contact-subtitle {
  color: #64748b;
  font-size: 1rem;
  margin: 0 0 1.5rem 0;
  line-height: 1.5;
}

.contact-methods {
  margin-bottom: 2rem;
}

.contact-item {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
  margin-bottom: 1.5rem;
  padding-bottom: 1rem;
  border-bottom: 1px solid #f1f5f9;
}

.contact-item:last-child {
  margin-bottom: 0;
  border-bottom: none;
  padding-bottom: 0;
}

.contact-icon {
  width: 48px;
  height: 48px;
  border-radius: 12px;
  background: linear-gradient(135deg, #3b82f6 0%, #1e40af 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-size: 1.25rem;
  flex-shrink: 0;
  box-shadow: 0 4px 8px rgba(59, 130, 246, 0.2);
}

.contact-content {
  flex: 1;
  min-width: 0;
}

.contact-label {
  font-weight: 600;
  color: #374151;
  font-size: 0.875rem;
  margin-bottom: 0.25rem;
  text-transform: uppercase;
  letter-spacing: 0.025em;
}

.contact-link {
  display: block;
  color: #1e293b;
  font-size: 1.125rem;
  font-weight: 600;
  text-decoration: none;
  margin-bottom: 0.25rem;
  transition: color 0.2s ease;
}

.contact-link:hover,
.contact-link:focus {
  color: #3b82f6;
  outline: none;
  text-decoration: underline;
}

.contact-value {
  color: #1e293b;
  font-size: 1rem;
  font-weight: 500;
  line-height: 1.4;
}

.hours-note {
  font-size: 0.875rem;
  color: #6b7280;
  font-weight: 400;
  margin-top: 0.25rem;
}

.contact-desc {
  font-size: 0.875rem;
  color: #6b7280;
  line-height: 1.4;
}

.contact-trust {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
  margin-bottom: 1.5rem;
  padding: 1rem;
  background: #f8fafc;
  border-radius: 8px;
  border-left: 4px solid #10b981;
}

.trust-badge {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  font-size: 0.875rem;
  color: #374151;
}

.trust-badge i {
  color: #10b981;
  width: 16px;
  text-align: center;
}

.contact-cta {
  text-align: center;
}

.btn-contact {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.875rem 1.75rem;
  background: linear-gradient(135deg, #10b981 0%, #059669 100%);
  color: white;
  text-decoration: none;
  border-radius: 8px;
  font-weight: 600;
  font-size: 1rem;
  transition: all 0.2s ease;
  box-shadow: 0 2px 8px rgba(16, 185, 129, 0.3);
}

.btn-contact:hover {
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(16, 185, 129, 0.4);
  color: white;
}

.btn-contact:focus {
  outline: none;
  box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.5);
}

.btn-contact:active {
  transform: translateY(0);
}

.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

/* Mobile responsiveness */
@media (max-width: 768px) {
  .contact-card {
    position: static;
    padding: 1.5rem;
    margin-top: 2rem;
  }

  .contact-item {
    flex-direction: row;
    text-align: left;
  }

  .contact-icon {
    width: 40px;
    height: 40px;
    font-size: 1.125rem;
  }

  .contact-trust {
    margin: 1rem 0;
  }

  .trust-badge {
    justify-content: center;
  }
}

@media (max-width: 480px) {
  .contact-methods {
    margin-bottom: 1.5rem;
  }

  .contact-item {
    margin-bottom: 1rem;
    padding-bottom: 0.75rem;
  }

  .contact-link {
    font-size: 1rem;
  }

  .btn-contact {
    width: 100%;
    justify-content: center;
  }
}

/* High contrast mode support */
@media (prefers-contrast: high) {
  .contact-card {
    border: 2px solid #000;
  }

  .contact-icon {
    background: #000;
  }

  .contact-trust {
    border-left-color: #000;
  }
}

/* Reduced motion support */
@media (prefers-reduced-motion: reduce) {
  .btn-contact:hover {
    transform: none;
  }

  .btn-contact:active {
    transform: none;
  }
}
</style>